services:
  - type: web
    name: occu-med-insight-hub
    env: node
    plan: free
    buildCommand: pnpm install --no-frozen-lockfile && pnpm --filter @workspace/db run push && pnpm --filter @workspace/occu-med-insight-hub run build && pnpm --filter @workspace/api-server run build
    startCommand: pnpm --filter @workspace/api-server run start
    healthCheckPath: /api/healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20
      - key: BASE_PATH
        value: /
      # External Postgres/Neon connection string.
      # Set this in the Render dashboard; do not commit database credentials.
      - key: DATABASE_URL
        sync: false
      # --- Third-party API keys (set values in Render dashboard, never in code) ---
      - key: APIFY_API_KEY
        sync: false
      - key: JINA_API_KEY
        sync: false
      - key: OCR_SPACE_API_KEY
        sync: false
      - key: LANGSEARCH_API_KEY
        sync: false
      - key: SERPER_API_KEY
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
      - key: EXA_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
      - key: FIRECRAWL_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: OLOSTEP_API_KEY
        sync: false
      - key: YOU_API_KEY
        sync: false
