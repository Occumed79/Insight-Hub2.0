name: Patch AOR Operational Priority

on:
  push:
    branches:
      - fix/aor-operational-priority

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v6
        with:
          ref: fix/aor-operational-priority
          fetch-depth: 0

      - name: Apply AOR architecture and data-quality fixes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re, json

          root = Path('.')
          page = root / 'artifacts/occu-med-insight-hub/src/pages/reviewer-aor-factors-v2.tsx'
          backend = root / 'artifacts/api-server/src/routes/aor-production-repair.ts'
          crisis = root / 'artifacts/api-server/src/routes/crisiswatch.ts'
          tests = root / 'artifacts/occu-med-insight-hub/tests/aor-consolidation.spec.ts'
          baseline = root / 'docs/repository-cleanup-baseline.json'
          priority_component = root / 'artifacts/occu-med-insight-hub/src/components/insight/AorPriorityBrief.tsx'
          registry = root / 'artifacts/occu-med-insight-hub/src/components/insight/aor-command-registry.ts'

          priority_component.write_text(r'''import { AlertTriangle, ArrowUpRight, CircleDot, RadioTower } from "lucide-react";

export type AorPriorityLevel = "critical" | "elevated" | "monitor" | "context";
export type AorPrioritySignal = {
  id: string;
  level: AorPriorityLevel;
  source: string;
  title: string;
  detail?: string;
  timestamp?: string;
  href?: string;
};

type PriorityInput = {
  advisory?: any;
  outbreaks?: any[];
  disasters?: any[];
  earthquakes?: any[];
  crisisUpdates?: any[];
  healthNotices?: string[];
  environmentLabels?: string[];
};

const LEVEL_RANK: Record<AorPriorityLevel, number> = { critical: 0, elevated: 1, monitor: 2, context: 3 };

function safeUrl(value?: string) {
  if (!value) return "";
  try { const parsed = new URL(value); return parsed.protocol === "https:" ? parsed.toString() : ""; } catch { return ""; }
}

function signal(id: string, level: AorPriorityLevel, source: string, title: string, detail?: string, timestamp?: string, href?: string): AorPrioritySignal {
  return { id, level, source, title, detail, timestamp, href: safeUrl(href) || undefined };
}

function advisoryLevel(raw: unknown): AorPriorityLevel {
  const level = Number(raw);
  if (level >= 4) return "critical";
  if (level >= 3) return "elevated";
  if (level >= 2) return "monitor";
  return "context";
}

function gdacsLevel(raw: unknown): AorPriorityLevel {
  const level = String(raw || "").toUpperCase();
  if (level === "RED") return "critical";
  if (level === "ORANGE") return "elevated";
  if (level === "GREEN") return "monitor";
  return "context";
}

function earthquakeLevel(item: any): AorPriorityLevel {
  if (item?.tsunami) return "critical";
  const magnitude = Number(item?.magnitude);
  if (Number.isFinite(magnitude) && magnitude >= 7) return "critical";
  if (Number.isFinite(magnitude) && magnitude >= 6) return "elevated";
  if (Number.isFinite(magnitude) && magnitude >= 5) return "monitor";
  return "context";
}

export function buildAorPrioritySignals(input: PriorityInput): AorPrioritySignal[] {
  const items: AorPrioritySignal[] = [];
  const advisory = input.advisory;
  if (advisory) {
    items.push(signal(
      `state-${advisory.level || "advisory"}`,
      advisoryLevel(advisory.level),
      "U.S. Department of State",
      `Level ${advisory.level ?? "?"} · ${advisory.levelLabel || "Travel advisory"}`,
      advisory.summary || advisory.details || "Review the official travel advisory.",
      advisory.updatedAt || advisory.date,
      advisory.sourceUrl,
    ));
  }

  for (const [index, item] of (input.disasters || []).slice(0, 8).entries()) {
    items.push(signal(
      `gdacs-${item.eventId || item.id || index}`,
      gdacsLevel(item.alertLevel),
      "GDACS",
      `${String(item.alertLevel || "Alert").toUpperCase()} · ${item.name || item.title || "Natural hazard"}`,
      item.description || item.country,
      item.fromDate || item.publishedAt,
      item.sourceUrl || item.url,
    ));
  }

  for (const [index, item] of (input.earthquakes || []).slice(0, 8).entries()) {
    const magnitude = Number(item.magnitude);
    items.push(signal(
      `usgs-${item.id || index}`,
      earthquakeLevel(item),
      "USGS",
      item.title || item.place || (Number.isFinite(magnitude) ? `M${magnitude.toFixed(1)} earthquake` : "Earthquake"),
      `${Number.isFinite(magnitude) ? `Magnitude ${magnitude.toFixed(1)}` : "Magnitude not supplied"}${item.depthKm == null ? "" : ` · ${item.depthKm} km depth`}${item.tsunami ? " · tsunami flag" : ""}`,
      item.occurredAt,
      item.url,
    ));
  }

  for (const [index, item] of (input.outbreaks || []).slice(0, 6).entries()) {
    items.push(signal(
      `who-${item.id || index}`,
      "monitor",
      "WHO Disease Outbreak News",
      item.title || "WHO disease outbreak update",
      item.summary,
      item.publicationDate || item.publishedAt,
      item.sourceUrl || item.url,
    ));
  }

  for (const [index, item] of (input.crisisUpdates || []).slice(0, 4).entries()) {
    items.push(signal(
      `crisis-${item.id || index}`,
      "monitor",
      "CrisisWatch",
      item.title || item.headline || "CrisisWatch update",
      item.summary || item.description,
      item.publishedAt || item.date,
      item.sourceUrl || item.url,
    ));
  }

  for (const [index, notice] of (input.healthNotices || []).slice(0, 4).entries()) {
    items.push(signal(`cdc-notice-${index}`, "monitor", "CDC Travelers' Health", notice, "Destination-specific travel health notice."));
  }

  if ((input.environmentLabels || []).length) {
    items.push(signal(
      "reviewer-environment",
      "context",
      "Reviewer-confirmed conditions",
      `${input.environmentLabels!.length} environmental / human-performance factor${input.environmentLabels!.length === 1 ? "" : "s"} selected`,
      input.environmentLabels!.join(" · "),
    ));
  }

  return items
    .sort((a, b) => LEVEL_RANK[a.level] - LEVEL_RANK[b.level] || String(b.timestamp || "").localeCompare(String(a.timestamp || "")))
    .slice(0, 10);
}

const levelTone: Record<AorPriorityLevel, string> = {
  critical: "border-rose-200/24 bg-rose-300/[0.08] text-rose-50",
  elevated: "border-amber-200/24 bg-amber-300/[0.08] text-amber-50",
  monitor: "border-cyan-200/20 bg-cyan-300/[0.055] text-cyan-50",
  context: "border-white/11 bg-white/[0.025] text-white/72",
};

const levelLabel: Record<AorPriorityLevel, string> = {
  critical: "Critical",
  elevated: "Elevated",
  monitor: "Monitor",
  context: "Context",
};

export function AorPriorityBrief({ context, signals, loading = false, error = "" }: { context: string; signals: AorPrioritySignal[]; loading?: boolean; error?: string }) {
  const actionCount = signals.filter((item) => item.level === "critical" || item.level === "elevated").length;
  const monitorCount = signals.filter((item) => item.level === "monitor").length;
  return (
    <section data-testid="aor-priority-brief" className="rounded-[24px] border border-cyan-100/14 bg-gradient-to-br from-cyan-300/[0.07] via-[#03101b]/76 to-violet-300/[0.06] p-4 shadow-[0_18px_55px_rgba(0,0,0,.24)] backdrop-blur-2xl md:p-5">
      <div className="flex flex-wrap items-start justify-between gap-3">
        <div>
          <p className="text-[9px] font-black uppercase tracking-[0.2em] text-cyan-100/46">Operational Priority Brief</p>
          <h2 className="mt-1 text-lg font-black text-white">{context}</h2>
          <p className="mt-1 max-w-3xl text-[10px] leading-5 text-cyan-100/42">Source-defined triage keeps urgent source signals above background context. It does not combine unrelated feeds into a fabricated danger score.</p>
        </div>
        <div className="flex gap-2 text-[9px] font-black uppercase tracking-[0.12em]">
          <span className="rounded-full border border-amber-100/16 bg-amber-300/[0.055] px-2.5 py-1 text-amber-50/72">{actionCount} action</span>
          <span className="rounded-full border border-cyan-100/16 bg-cyan-300/[0.055] px-2.5 py-1 text-cyan-50/72">{monitorCount} monitor</span>
        </div>
      </div>

      {error ? <div className="mt-4 rounded-xl border border-amber-200/16 bg-amber-300/[0.045] p-3 text-[10px] leading-5 text-amber-100/72"><AlertTriangle size={13} className="mr-2 inline" />{error}</div> : null}
      {loading && !signals.length ? <div className="mt-4 flex min-h-24 items-center justify-center gap-2 text-xs text-cyan-100/46"><RadioTower size={15} className="animate-pulse" />Loading current source signals…</div> : signals.length ? (
        <div className="mt-4 grid gap-2 lg:grid-cols-2 xl:grid-cols-3">
          {signals.slice(0, 6).map((item) => (
            <article key={item.id} className={`rounded-2xl border p-3.5 ${levelTone[item.level]}`}>
              <div className="flex items-center justify-between gap-2"><span className="inline-flex items-center gap-1.5 text-[8px] font-black uppercase tracking-[0.12em]"><CircleDot size={9} />{levelLabel[item.level]}</span><span className="text-[8px] font-bold uppercase tracking-[0.1em] opacity-50">{item.source}</span></div>
              <h3 className="mt-2 text-[11px] font-black leading-4">{item.title}</h3>
              {item.detail ? <p className="mt-2 line-clamp-3 text-[9px] leading-4 opacity-60">{item.detail}</p> : null}
              {item.href ? <a href={item.href} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-1 text-[9px] font-black opacity-70 hover:opacity-100">Open source<ArrowUpRight size={9} /></a> : null}
            </article>
          ))}
        </div>
      ) : <div className="mt-4 rounded-2xl border border-white/8 bg-white/[0.02] p-4 text-[10px] leading-5 text-cyan-100/42">No current signal met a source-specific display rule for this selection. Raw source sections remain available below.</div>}
    </section>
  );
}
''')

          registry.write_text(r'''export const AOR_REGISTRY_REVIEWED_AT = "2026-08-19";

export const COMMANDS = [
  { id: "northcom", label: "USNORTHCOM", scope: "United States, Canada, Mexico, Greenland, The Bahamas, and assigned approaches", center: [-101, 46] as [number, number], zoom: 1.55, color: "#49e1df", countries: ["US", "CA", "MX", "GL", "BS", "PR", "VI"] },
  { id: "southcom", label: "USSOUTHCOM", scope: "Central America, South America, the Caribbean, and adjacent approaches", center: [-67, -9] as [number, number], zoom: 1.7, color: "#54f0bd", countries: ["AG", "AR", "BB", "BZ", "BO", "BR", "CL", "CO", "CR", "CU", "DM", "DO", "EC", "SV", "GD", "GT", "GY", "HT", "HN", "JM", "NI", "PA", "PY", "PE", "KN", "LC", "VC", "SR", "TT", "UY", "VE"] },
  { id: "eucom", label: "USEUCOM", scope: "Europe and assigned portions of Eurasia, the Arctic, Atlantic, and adjoining approaches", center: [21, 52] as [number, number], zoom: 2.15, color: "#65b9ff", countries: ["AL", "AD", "AM", "AT", "AZ", "BY", "BE", "BA", "BG", "HR", "CY", "CZ", "DK", "EE", "FI", "FR", "GE", "DE", "GR", "HU", "IS", "IE", "IT", "XK", "LV", "LI", "LT", "LU", "MT", "MD", "MC", "ME", "NL", "MK", "NO", "PL", "PT", "RO", "RU", "SM", "RS", "SK", "SI", "ES", "SE", "CH", "TR", "UA", "GB", "VA"] },
  { id: "africom", label: "USAFRICOM", scope: "The African continent, island nations, and surrounding waters, except Egypt", center: [19, 3] as [number, number], zoom: 1.8, color: "#a580ff", countries: ["DZ", "AO", "BJ", "BW", "BF", "BI", "CV", "CM", "CF", "TD", "KM", "CG", "CD", "CI", "DJ", "GQ", "ER", "SZ", "ET", "GA", "GM", "GH", "GN", "GW", "KE", "LS", "LR", "LY", "MG", "MW", "ML", "MR", "MU", "MA", "MZ", "NA", "NE", "NG", "RW", "ST", "SN", "SC", "SL", "SO", "ZA", "SS", "SD", "TZ", "TG", "TN", "UG", "ZM", "ZW"] },
  { id: "centcom", label: "USCENTCOM", scope: "Twenty-one nations across the Middle East and Central and South Asia, including Egypt", center: [53, 30] as [number, number], zoom: 2.25, color: "#54d8ff", countries: ["AF", "BH", "EG", "IR", "IQ", "IL", "JO", "KZ", "KW", "KG", "LB", "OM", "PK", "QA", "SA", "SY", "TJ", "TM", "AE", "UZ", "YE"] },
  { id: "indopacom", label: "USINDOPACOM", scope: "The Indo-Pacific from India through East Asia, Australia, and Pacific island nations", center: [142, 13] as [number, number], zoom: 1.45, color: "#7d78ff", countries: ["AU", "BD", "BT", "BN", "KH", "CN", "TW", "FJ", "IN", "ID", "JP", "KI", "LA", "MY", "MV", "MH", "FM", "MN", "MM", "NR", "NP", "NZ", "KP", "PW", "PG", "PH", "WS", "SG", "SB", "KR", "LK", "TH", "TL", "TO", "TV", "VU", "VN"] },
] as const;

export type Command = (typeof COMMANDS)[number];
export type CommandId = Command["id"];
export const COMMAND_BY_COUNTRY = new Map<string, Command>(COMMANDS.flatMap((command) => command.countries.map((iso2) => [iso2, command] as [string, Command])));
''')

          # Backend: global watch + bounding-box seismic endpoint.
          b = backend.read_text()
          insert = r'''
async function whoLatest() {
  return payloadItems(await whoFeed()).map(whoItem).sort((a, b) => (b.publicationDate || "").localeCompare(a.publicationDate || "")).slice(0, 8).map((item) => ({
    id: item.id,
    title: item.title,
    publicationDate: item.publicationDate,
    summary: item.summary,
    sourceUrl: item.sourceUrl,
  }));
}

async function gdacsGlobal() {
  const severity = (value: string) => ({ RED: 3, ORANGE: 2, GREEN: 1 }[String(value || "").toUpperCase()] || 0);
  const events = payloadItems(await fetchJson("https://www.gdacs.org/contentdata/xml/gdacs_app_feed.json")).map(parseGdacsEvent);
  return events.sort((a, b) => severity(b.alertLevel) - severity(a.alertLevel) || (b.fromDate || "").localeCompare(a.fromDate || "")).slice(0, 10).map((item) => ({
    eventType: item.eventType,
    eventId: item.eventId,
    name: item.title,
    description: item.description,
    alertLevel: item.alertLevel,
    fromDate: item.fromDate,
    toDate: item.toDate,
    country: item.country,
    affectedCountries: item.affectedCountries,
    latitude: item.latitude,
    longitude: item.longitude,
    sourceUrl: item.url,
  }));
}

function parseUsgsFeature(raw: unknown) {
  const feature = record(raw);
  const props = record(feature.properties);
  const geometry = record(feature.geometry);
  const coord = firstCoordinate(geometry.coordinates);
  const place = text(props.place);
  const magnitude = num(props.mag);
  return {
    id: text(feature.id) || text(props.code) || `${place}-${text(props.time)}`,
    title: text(props.title) || (magnitude === null ? `Earthquake near ${place || "unknown location"}` : `M${magnitude.toFixed(1)} · ${place || "earthquake"}`),
    place,
    magnitude,
    occurredAt: Number.isFinite(Number(props.time)) ? new Date(Number(props.time)).toISOString() : "",
    url: text(props.url) || "https://earthquake.usgs.gov/earthquakes/map/",
    tsunami: Number(props.tsunami) === 1,
    latitude: coord?.[1] ?? null,
    longitude: coord?.[0] ?? null,
    depthKm: Array.isArray(geometry.coordinates) ? num(geometry.coordinates[2]) : null,
  };
}

async function usgsGlobal() {
  const start = new Date(Date.now() - 7 * 86_400_000).toISOString();
  const params = new URLSearchParams({ format: "geojson", starttime: start, minmagnitude: "5.5", orderby: "time", limit: "100" });
  const payload = record(await fetchJson(`https://earthquake.usgs.gov/fdsnws/event/1/query?${params}`));
  return array(payload.features).map(parseUsgsFeature).sort((a, b) => Number(b.tsunami) - Number(a.tsunami) || (b.magnitude ?? 0) - (a.magnitude ?? 0) || (b.occurredAt || "").localeCompare(a.occurredAt || "")).slice(0, 10);
}

async function usgsForBounds(bounds: [number, number, number, number], days: number) {
  const [minLon, minLat, maxLon, maxLat] = bounds;
  const start = new Date(Date.now() - days * 86_400_000).toISOString();
  const params = new URLSearchParams({
    format: "geojson",
    starttime: start,
    minmagnitude: "4.0",
    orderby: "time",
    limit: "200",
    minlongitude: String(minLon),
    minlatitude: String(minLat),
    maxlongitude: String(maxLon),
    maxlatitude: String(maxLat),
  });
  const payload = record(await fetchJson(`https://earthquake.usgs.gov/fdsnws/event/1/query?${params}`));
  return array(payload.features).map(parseUsgsFeature).slice(0, 20);
}
'''
          marker = '\nasync function unifiedCommand(req: Request, res: Response) {'
          if insert.strip() not in b:
              if marker not in b: raise SystemExit('backend unifiedCommand marker missing')
              b = b.replace(marker, '\n' + insert + marker, 1)

          routes = r'''
router.get("/aor/global-watch", async (_req, res) => {
  res.setHeader("Cache-Control", "no-store");
  const [who, gdacs, usgs] = await Promise.allSettled([whoLatest(), gdacsGlobal(), usgsGlobal()]);
  const sourceHealth = [
    { provider: "WHO Disease Outbreak News", ok: who.status === "fulfilled", count: who.status === "fulfilled" ? who.value.length : 0, ...(who.status === "rejected" ? { error: who.reason instanceof Error ? who.reason.message : "WHO unavailable." } : {}) },
    { provider: "GDACS", ok: gdacs.status === "fulfilled", count: gdacs.status === "fulfilled" ? gdacs.value.length : 0, ...(gdacs.status === "rejected" ? { error: gdacs.reason instanceof Error ? gdacs.reason.message : "GDACS unavailable." } : {}) },
    { provider: "USGS Earthquake Catalog", ok: usgs.status === "fulfilled", count: usgs.status === "fulfilled" ? usgs.value.length : 0, ...(usgs.status === "rejected" ? { error: usgs.reason instanceof Error ? usgs.reason.message : "USGS unavailable." } : {}) },
  ];
  return res.json({
    ok: true,
    retrievedAt: new Date().toISOString(),
    partial: sourceHealth.some((item) => !item.ok),
    sourceHealth,
    outbreaks: who.status === "fulfilled" ? who.value : [],
    disasters: gdacs.status === "fulfilled" ? gdacs.value : [],
    earthquakes: usgs.status === "fulfilled" ? usgs.value : [],
    limitation: "Global watch is a source-defined starting view. WHO items are recent publication context, GDACS retains its alert levels, and USGS events use magnitude/tsunami fields; no cross-source danger score is calculated.",
  });
});

router.get("/aor/seismic-activity", async (req, res) => {
  res.setHeader("Cache-Control", "no-store");
  const values = [req.query.minLon, req.query.minLat, req.query.maxLon, req.query.maxLat].map(Number);
  if (!values.every(Number.isFinite)) return res.status(400).json({ ok: false, error: "minLon, minLat, maxLon, and maxLat are required numeric bounds" });
  const [minLon, minLat, maxLon, maxLat] = values;
  if (minLon < -180 || maxLon > 180 || minLat < -90 || maxLat > 90 || minLon >= maxLon || minLat >= maxLat) return res.status(400).json({ ok: false, error: "invalid geographic bounds" });
  const rawDays = Number(req.query.days);
  const days = Number.isFinite(rawDays) ? Math.min(Math.max(Math.trunc(rawDays), 1), 90) : 30;
  try {
    const earthquakes = await usgsForBounds([minLon, minLat, maxLon, maxLat], days);
    return res.json({ ok: true, days, bounds: [minLon, minLat, maxLon, maxLat], earthquakes, source: "USGS Earthquake Catalog", sourceUrl: "https://earthquake.usgs.gov/fdsnws/event/1/", limitation: "Country-mode seismic events are bounded geographically using the selected country geometry/bounding box rather than inferred from command-wide place-name text." });
  } catch (error) {
    return res.status(502).json({ ok: false, error: error instanceof Error ? error.message : "USGS unavailable." });
  }
});
'''
          export_marker = '\nexport default router;'
          if '/aor/global-watch' not in b:
              if export_marker not in b: raise SystemExit('backend export marker missing')
              b = b.replace(export_marker, '\n' + routes + export_marker, 1)
          backend.write_text(b)

          # CrisisWatch: never substitute unrelated global items for a selected country.
          c = crisis.read_text()
          c = c.replace('    const selected = (directMatches.length > 0 ? directMatches : loaded.items.slice(0, 12)).slice(0, 20);', '    const selected = directMatches.slice(0, 20);')
          c = c.replace('      fallbackUsed: directMatches.length === 0,', '      fallbackUsed: false,\n      unrelatedItemsOmitted: Math.max(0, loaded.items.length - directMatches.length),')
          c = c.replace('limitation: "CrisisWatch is qualitative early-warning analysis rather than event-level incident data. Country matching is text-based, cross-border conflicts may appear under regional names, and all summaries should be reviewed against the linked source.",', 'limitation: "CrisisWatch is qualitative early-warning analysis rather than event-level incident data. Country matching is text-based; unrelated global feed items are intentionally omitted when the selected country has no direct match, and cross-border conflicts may appear under regional names.",')
          crisis.write_text(c)

          # Frontend page imports, registry extraction, global watch, bounded USGS, priority brief.
          p = page.read_text()
          p = p.replace('import { GlassCard } from "@/components/insight/GlassCard";\n', 'import { GlassCard } from "@/components/insight/GlassCard";\nimport { AorPriorityBrief, buildAorPrioritySignals } from "@/components/insight/AorPriorityBrief";\nimport { AOR_REGISTRY_REVIEWED_AT, COMMANDS, COMMAND_BY_COUNTRY, type CommandId } from "@/components/insight/aor-command-registry";\n')
          pattern = re.compile(r'const COMMANDS = \[.*?const COMMAND_BY_COUNTRY = new Map<string, Command>\(COMMANDS\.flatMap\(\(command\) => command\.countries\.map\(\(iso2\) => \[iso2, command\] as \[string, Command\]\)\)\);\n', re.S)
          replacement = '''type MapMode = "country" | "aor";\ntype SelectedCountry = { name: string; iso2: string; center?: [number, number]; bbox?: [number, number, number, number] };\ntype SourceResult = { data: any; error: string; loading: boolean };\ntype CountrySources = { travel: SourceResult; who: SourceResult; gdacs: SourceResult; usgs: SourceResult; crisiswatch: SourceResult; health: SourceResult };\ntype EnvironmentKey = "heat" | "cold" | "altitude" | "poorAir" | "fatigue" | "ppe" | "night";\n\ntype AorResponse = {\n  ok: boolean;\n  command: CommandId;\n  commandLabel: string;\n  partial: boolean;\n  sourceHealth: Array<{ provider: string; ok: boolean; count: number; error?: string }>;\n  outbreaks: Array<any>;\n  disasters: Array<any>;\n  earthquakes: Array<any>;\n};\n\ntype GlobalWatchResponse = {\n  ok: boolean;\n  partial: boolean;\n  sourceHealth: Array<{ provider: string; ok: boolean; count: number; error?: string }>;\n  outbreaks: Array<any>;\n  disasters: Array<any>;\n  earthquakes: Array<any>;\n};\n'''
          p, n = pattern.subn(replacement, p, count=1)
          if n != 1: raise SystemExit(f'command registry block replacement count={n}')
          p = p.replace('function emptyCountrySources(): CountrySources { return { travel: emptyResult(), who: emptyResult(), gdacs: emptyResult(), crisiswatch: emptyResult(), health: emptyResult() }; }', 'function emptyCountrySources(): CountrySources { return { travel: emptyResult(), who: emptyResult(), gdacs: emptyResult(), usgs: emptyResult(), crisiswatch: emptyResult(), health: emptyResult() }; }')

          geometry_helper = r'''
function geometryBbox(geometry: any): [number, number, number, number] | undefined {
  const pairs: Array<[number, number]> = [];
  const walk = (value: any) => {
    if (!Array.isArray(value)) return;
    if (value.length >= 2 && Number.isFinite(Number(value[0])) && Number.isFinite(Number(value[1]))) {
      const lon = Number(value[0]); const lat = Number(value[1]);
      if (Math.abs(lon) <= 180 && Math.abs(lat) <= 90) pairs.push([lon, lat]);
      return;
    }
    value.forEach(walk);
  };
  walk(geometry?.coordinates);
  if (!pairs.length) return undefined;
  const lons = pairs.map(([lon]) => lon); const lats = pairs.map(([, lat]) => lat);
  const minLon = Math.min(...lons); const maxLon = Math.max(...lons); const minLat = Math.min(...lats); const maxLat = Math.max(...lats);
  if (minLon >= maxLon || minLat >= maxLat) return undefined;
  return [minLon, minLat, maxLon, maxLat];
}
'''
          if 'function geometryBbox' not in p:
              p = p.replace('function normalize(value: string) { return value.toLowerCase().replace(/[^a-z0-9]+/g, " ").trim(); }\n', 'function normalize(value: string) { return value.toLowerCase().replace(/[^a-z0-9]+/g, " ").trim(); }\n' + geometry_helper)

          p = p.replace('  const [countrySources, setCountrySources] = useState<CountrySources>(emptyCountrySources());\n', '  const [countrySources, setCountrySources] = useState<CountrySources>(emptyCountrySources());\n  const [globalWatch, setGlobalWatch] = useState<GlobalWatchResponse | null>(null);\n  const [globalWatchLoading, setGlobalWatchLoading] = useState(true);\n  const [globalWatchError, setGlobalWatchError] = useState("");\n')

          global_effect = r'''
  useEffect(() => {
    let active = true;
    setGlobalWatchLoading(true);
    setGlobalWatchError("");
    loadJson("/api/aor/global-watch")
      .then((payload) => { if (active) setGlobalWatch(payload); })
      .catch((reason) => { if (active) setGlobalWatchError(errorMessage(reason)); })
      .finally(() => { if (active) setGlobalWatchLoading(false); });
    return () => { active = false; };
  }, []);

'''
          mode_effect = '  useEffect(() => { modeRef.current = mapMode; }, [mapMode]);\n\n'
          if global_effect.strip() not in p:
              if mode_effect not in p: raise SystemExit('mode effect marker missing')
              p = p.replace(mode_effect, mode_effect + global_effect, 1)

          old_load = '''    setCountrySources({ travel: loadingResult, who: loadingResult, gdacs: loadingResult, crisiswatch: loadingResult, health: loadingResult });\n    const country = encodeURIComponent(selectedCountry.name);\n    const requests: Record<keyof CountrySources, Promise<any>> = {\n      travel: loadJson(`/api/public-data/aor-risk?country=${country}`),\n      who: loadJson(`/api/aor/health-outbreaks?country=${country}`),\n      gdacs: loadJson(`/api/aor/disaster-alerts?country=${country}&days=90`),\n      crisiswatch: loadJson(`/api/aor/crisiswatch?country=${country}`),\n      health: loadJson(`/api/aor/travel-health?country=${country}`),\n    };'''
          new_load = '''    setCountrySources({ travel: loadingResult, who: loadingResult, gdacs: loadingResult, usgs: loadingResult, crisiswatch: loadingResult, health: loadingResult });\n    const country = encodeURIComponent(selectedCountry.name);\n    const bbox = selectedCountry.bbox;\n    const seismic = bbox ? loadJson(`/api/aor/seismic-activity?minLon=${encodeURIComponent(String(bbox[0]))}&minLat=${encodeURIComponent(String(bbox[1]))}&maxLon=${encodeURIComponent(String(bbox[2]))}&maxLat=${encodeURIComponent(String(bbox[3]))}&days=30`) : Promise.resolve({ ok: true, earthquakes: [], limitation: "No country geometry bounds were available for this selection." });\n    const requests: Record<keyof CountrySources, Promise<any>> = {\n      travel: loadJson(`/api/public-data/aor-risk?country=${country}`),\n      who: loadJson(`/api/aor/health-outbreaks?country=${country}`),\n      gdacs: loadJson(`/api/aor/disaster-alerts?country=${country}&days=90`),\n      usgs: seismic,\n      crisiswatch: loadJson(`/api/aor/crisiswatch?country=${country}`),\n      health: loadJson(`/api/aor/travel-health?country=${country}`),\n    };'''
          if old_load not in p: raise SystemExit('country source load block missing')
          p = p.replace(old_load, new_load, 1)
          p = p.replace('  }, [selectedCountry?.name]);', '  }, [selectedCountry?.name, selectedCountry?.bbox]);', 1)

          old_click = '''            const properties = event.features?.[0]?.properties ?? {};\n            const iso2 = String(properties.iso_a2 || "").toUpperCase();\n            const name = String(properties["name:en"] || properties.name || properties.name_en || iso2 || "Selected country");'''
          new_click = '''            const feature = event.features?.[0];\n            const properties = feature?.properties ?? {};\n            const iso2 = String(properties.iso_a2 || "").toUpperCase();\n            const name = String(properties["name:en"] || properties.name || properties.name_en || iso2 || "Selected country");\n            const featureBbox = geometryBbox(feature?.geometry);'''
          if old_click not in p: raise SystemExit('map click country block missing')
          p = p.replace(old_click, new_click, 1)
          p = p.replace('            setSelectedCountry({ name, iso2 });', '            setSelectedCountry({ name, iso2, bbox: featureBbox });', 1)

          old_quakes = '''  const strictQuakes = useMemo(() => {\n    const quakes = data?.earthquakes || [];\n    if (!selectedCountry) return mapMode === "aor" ? quakes.slice(0, 6) : [];\n    const needle = normalize(selectedCountry.name);\n    return quakes.filter((item) => normalize(`${item.place || ""} ${item.title || ""}`).includes(needle)).slice(0, 6);\n  }, [data?.earthquakes, mapMode, selectedCountry]);'''
          new_quakes = '''  const strictQuakes = useMemo(() => {\n    if (selectedCountry) return (countrySources.usgs.data?.earthquakes || []).slice(0, 6);\n    return mapMode === "aor" ? (data?.earthquakes || []).slice(0, 6) : [];\n  }, [countrySources.usgs.data?.earthquakes, data?.earthquakes, mapMode, selectedCountry]);'''
          if old_quakes not in p: raise SystemExit('strictQuakes block missing')
          p = p.replace(old_quakes, new_quakes, 1)

          priority_logic = r'''
  const prioritySignals = useMemo(() => buildAorPrioritySignals({
    advisory: selectedCountry ? advisory : undefined,
    outbreaks: selectedCountry ? countryWho : mapMode === "aor" ? (data?.outbreaks || []) : (globalWatch?.outbreaks || []),
    disasters: selectedCountry ? countryGdacs : mapMode === "aor" ? (data?.disasters || []) : (globalWatch?.disasters || []),
    earthquakes: selectedCountry ? strictQuakes : mapMode === "aor" ? (data?.earthquakes || []) : (globalWatch?.earthquakes || []),
    crisisUpdates: selectedCountry ? crisisUpdates.filter((item: any) => item.matchedCountry !== false) : [],
    healthNotices: selectedCountry ? (travelHealth?.notices || []) : [],
    environmentLabels: selectedEnvironment.map((key) => ENVIRONMENT_LABELS[key]),
  }), [advisory, countryGdacs, countryWho, crisisUpdates, data?.disasters, data?.earthquakes, data?.outbreaks, globalWatch?.disasters, globalWatch?.earthquakes, globalWatch?.outbreaks, mapMode, selectedCountry, selectedEnvironment, strictQuakes, travelHealth?.notices]);
  const priorityContext = selectedCountry?.name || (mapMode === "aor" ? selectedCommand.label : "Global watch");
  const priorityLoading = selectedCountry ? Object.values(countrySources).some((source) => source.loading) : mapMode === "aor" ? loading : globalWatchLoading;
  const priorityError = selectedCountry ? "" : mapMode === "aor" ? error : globalWatchError;
'''
          insert_after = '  const vaccines = (travelHealth?.vaccines || []).slice(0, 10);\n  const infectiousDiseases = (travelHealth?.diseases || []).slice(0, 10);\n'
          if priority_logic.strip() not in p:
              if insert_after not in p: raise SystemExit('priority insert marker missing')
              p = p.replace(insert_after, insert_after + priority_logic, 1)

          status_old = '''  const healthStatus = countrySourceStatus(countrySources.health);\n  const stateStatus = countrySourceStatus(countrySources.travel);\n  const crisisStatus = countrySourceStatus(countrySources.crisiswatch);'''
          status_new = '''  const healthStatus = countrySourceStatus(countrySources.health);\n  const stateStatus = countrySourceStatus(countrySources.travel);\n  const whoStatus = countrySourceStatus(countrySources.who);\n  const gdacsStatus = countrySourceStatus(countrySources.gdacs);\n  const usgsStatus = countrySourceStatus(countrySources.usgs);\n  const crisisStatus = countrySourceStatus(countrySources.crisiswatch);'''
          if status_old not in p: raise SystemExit('status block missing')
          p = p.replace(status_old, status_new, 1)

          chip_old = '''            {mapMode === "aor" ? ["WHO Disease Outbreak News", "GDACS", "USGS Earthquake Catalog"].map((provider) => { const source = sourceHealth.get(provider); return <SourceChip key={provider} label={provider.replace(" Disease Outbreak News", "").replace(" Earthquake Catalog", "")} status={loading ? "loading" : source?.ok ? "ok" : "warn"} note={loading ? "Refreshing" : source?.ok ? `${source.count} AOR matches` : source?.error || "Unavailable"} />; }) : <><SourceChip label="CDC Travel Health" status={healthStatus.status} note={healthStatus.note} /><SourceChip label="State Travel" status={stateStatus.status} note={stateStatus.note} /><SourceChip label="CrisisWatch" status={crisisStatus.status} note={crisisStatus.note} /></>}\n          </div>'''
          chip_new = '''            {mapMode === "aor" ? ["WHO Disease Outbreak News", "GDACS", "USGS Earthquake Catalog"].map((provider) => { const source = sourceHealth.get(provider); return <SourceChip key={provider} label={provider.replace(" Disease Outbreak News", "").replace(" Earthquake Catalog", "")} status={loading ? "loading" : source?.ok ? "ok" : "warn"} note={loading ? "Refreshing" : source?.ok ? `${source.count} AOR matches` : source?.error || "Unavailable"} />; }) : <><SourceChip label="State Travel" status={stateStatus.status} note={stateStatus.note} /><SourceChip label="CDC Travel Health" status={healthStatus.status} note={healthStatus.note} /><SourceChip label="WHO" status={whoStatus.status} note={whoStatus.note} /><SourceChip label="GDACS" status={gdacsStatus.status} note={gdacsStatus.note} /><SourceChip label="USGS" status={usgsStatus.status} note={usgsStatus.note} /><SourceChip label="CrisisWatch" status={crisisStatus.status} note={crisisStatus.note} /></>}\n          </div>\n\n          <div className="mt-4">\n            <AorPriorityBrief context={priorityContext} signals={prioritySignals} loading={priorityLoading} error={priorityError} />\n          </div>'''
          if chip_old not in p: raise SystemExit('source chip block missing')
          p = p.replace(chip_old, chip_new, 1)

          p = p.replace('You can use these controls or click any country on the map; the map will resolve that country to its assigned AOR.</p>', 'You can use these controls or click any country on the map; the map will resolve that country to its assigned AOR. Assignment registry reviewed {AOR_REGISTRY_REVIEWED_AT}.</p>', 1)
          p = p.replace('No command-feed earthquake place text matched ${selectedCountry.name}; unrelated command earthquakes are not substituted.', 'No USGS event fell inside the selected country bounds; command-wide earthquakes are not substituted.', 1)
          page.write_text(p)

          # Browser fixtures for global watch and bounded country seismic data.
          t = tests.read_text()
          global_fixture = r'''
const globalWatchFixture = {
  ok: true,
  partial: false,
  sourceHealth: [
    { provider: "WHO Disease Outbreak News", ok: true, count: 1 },
    { provider: "GDACS", ok: true, count: 1 },
    { provider: "USGS Earthquake Catalog", ok: true, count: 1 },
  ],
  outbreaks: [{ id: "global-who", title: "Global WHO watch item", publicationDate: new Date().toISOString(), summary: "Global outbreak context", sourceUrl: "https://www.who.int/" }],
  disasters: [{ eventId: "global-red", name: "Test cyclone requiring attention", alertLevel: "RED", fromDate: new Date().toISOString(), country: "Testland", latitude: 12, longitude: 22, sourceUrl: "https://www.gdacs.org/" }],
  earthquakes: [{ id: "global-usgs", title: "M6.4 · Global test earthquake", place: "Global test location", magnitude: 6.4, occurredAt: new Date().toISOString(), url: "https://earthquake.usgs.gov/", tsunami: false, latitude: 5, longitude: 6, depthKm: 12 }],
};

const kuwaitSeismicFixture = {
  ok: true,
  earthquakes: [{ id: "kw-usgs", title: "M5.1 · Kuwait test earthquake", place: "Kuwait", magnitude: 5.1, occurredAt: new Date().toISOString(), url: "https://earthquake.usgs.gov/", tsunami: false, latitude: 29.2, longitude: 47.4, depthKm: 9 }],
};
'''
          marker = '\nconst cdcFixture = {'
          if 'const globalWatchFixture' not in t:
              if marker not in t: raise SystemExit('test fixture marker missing')
              t = t.replace(marker, '\n' + global_fixture + marker, 1)
          route_marker = '  await page.route("**/api/aor/unified-command?**", async (route) => route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify(aorFixture) }));\n'
          extra_routes = route_marker + '  await page.route("**/api/aor/global-watch", async (route) => route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify(globalWatchFixture) }));\n  await page.route("**/api/aor/seismic-activity?**", async (route) => route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify(kuwaitSeismicFixture) }));\n'
          if 'api/aor/global-watch' not in t:
              if route_marker not in t: raise SystemExit('test route marker missing')
              t = t.replace(route_marker, extra_routes, 1)
          t = t.replace('  await expect(page.getByText("Select a country to load its CDC travel-health profile.")).toBeVisible();', '  await expect(page.getByText("Select a country to load its CDC travel-health profile.")).toBeVisible();\n  await expect(page.getByText("Operational Priority Brief")).toBeVisible();\n  await expect(page.getByText("Global watch")).toBeVisible();\n  await expect(page.getByText("RED · Test cyclone requiring attention")).toBeVisible();', 1)
          t = t.replace('  await expect(page.getByText("Level 1 · Exercise Normal Precautions")).toBeVisible();', '  await expect(page.getByText("Level 1 · Exercise Normal Precautions")).toBeVisible();\n  await expect(page.getByText("M5.1 · Kuwait test earthquake")).toBeVisible();', 1)
          t = t.replace('  await expect(page.getByText(/unrelated command earthquakes are not substituted/)).toBeVisible();', '  await expect(page.getByText(/command-wide earthquakes are not substituted/)).toBeVisible();', 1)
          tests.write_text(t)

          data = json.loads(baseline.read_text())
          data['maximums']['totalFiles'] = 272
          desc = data.get('description','')
          if 'AOR operational-priority hardening' not in desc:
              desc += ' AOR operational-priority hardening deliberately adds two focused frontend modules: a versioned command assignment registry and a source-defined priority brief. The AOR page now starts with a global WHO/GDACS/USGS watch view, uses country bounding boxes for USGS country-mode seismic queries, and suppresses unrelated CrisisWatch fallback items; the reviewed repository file ceiling is therefore 272 while every other structural guard remains unchanged.'
          data['description'] = desc
          for required in [
              'artifacts/occu-med-insight-hub/src/components/insight/AorPriorityBrief.tsx',
              'artifacts/occu-med-insight-hub/src/components/insight/aor-command-registry.ts',
          ]:
              if required not in data['requiredFiles']:
                  data['requiredFiles'].append(required)
          baseline.write_text(json.dumps(data, indent=2) + '\n')
          PY

          rm .github/workflows/patch-aor-operational-priority.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add artifacts/api-server/src/routes/aor-production-repair.ts \
                  artifacts/api-server/src/routes/crisiswatch.ts \
                  artifacts/occu-med-insight-hub/src/pages/reviewer-aor-factors-v2.tsx \
                  artifacts/occu-med-insight-hub/src/components/insight/AorPriorityBrief.tsx \
                  artifacts/occu-med-insight-hub/src/components/insight/aor-command-registry.ts \
                  artifacts/occu-med-insight-hub/tests/aor-consolidation.spec.ts \
                  docs/repository-cleanup-baseline.json \
                  .github/workflows/patch-aor-operational-priority.yml
          git commit -m 'Prioritize AOR operational intelligence'
          git push origin HEAD:fix/aor-operational-priority
